"use client";
import{useState}from"react";import{BarChart3,CheckCircle2,GitCompareArrows,LockKeyhole,ShieldCheck,WalletCards}from"lucide-react";import{authorize,type Environment,type Principal}from"@fuelcap/authz";import{seededHedgeDecision,seededHedgePolicy,seededPublishedHedgePlan,type HedgePortfolio}from"@fuelcap/hedge-optimizer";
const selected=seededHedgeDecision.portfolios.find(p=>p.portfolioId===seededHedgeDecision.selectedPortfolioId)!;const money=(v:number)=>`$${(v/100).toLocaleString("en-US",{maximumFractionDigits:0})}`;const percent=(v:number)=>`${(v/100).toFixed(1)}%`;
export function HedgeOptimizerWorkspace({organisationId,principal,environment}:{organisationId:string;principal:Principal;environment:Environment}){const[portfolio,setPortfolio]=useState<HedgePortfolio>(selected);const[action,setAction]=useState<"idle"|"drafted"|"denied">("idle");function draft(){const p=authorize({principal,environment,activeOrganisationId:organisationId,workspace:"risk-hedging",verb:"initiate"});setAction(p.allowed?"drafted":"denied");}return <section className="hedge-optimizer"><div className="section-heading hedge-optimizer__heading"><div><span className="section-kicker">P7-003 · selective optimisation</span><h2>Paper hedge portfolio optimiser</h2><p>Every bounded candidate combination is compared against premium, coverage, basis, liquidity and counterparty constraints.</p></div><span className="pricing-blocked">No broker connection</span></div><div className="hedge-optimizer__metrics"><article><GitCompareArrows size={17}/><span>Portfolios evaluated</span><strong>{seededHedgeDecision.portfolios.length}</strong><small>all non-empty subsets</small></article><article><WalletCards size={17}/><span>Selected premium</span><strong>{money(selected.premiumMinor)}</strong><small>budget {money(seededHedgePolicy.maximumPremiumMinor)}</small></article><article><BarChart3 size={17}/><span>Stress residual</span><strong>{money(selected.residualExposureMinor)}</strong><small>payoff {money(selected.stressPayoffMinor)}</small></article><article><CheckCircle2 size={17}/><span>Basis correlation</span><strong>{percent(selected.weightedBasisCorrelationBps)}</strong><small>minimum {percent(seededHedgePolicy.minimumBasisCorrelationBps)}</small></article></div><div className="hedge-optimizer__layout"><div className="hedge-optimizer__list"><div className="section-heading"><div><span className="section-kicker">Deterministic comparison</span><h3>Portfolio evidence</h3></div><span className="tenant-chip">{seededHedgeDecision.reasonCode}</span></div>{seededHedgeDecision.portfolios.map(item=><button key={item.portfolioId} className={portfolio.portfolioId===item.portfolioId?"hedge-optimizer__active":""} type="button" onClick={()=>setPortfolio(item)}><span><strong>{item.candidateIds.join(" + ")}</strong><small>{percent(item.coverageBps)} coverage · {money(item.premiumMinor)} premium</small></span><span><small>Residual</small><strong>{money(item.residualExposureMinor)}</strong></span><span><small>Objective</small><strong>{item.objectiveScore.toLocaleString()}</strong></span><b>{item.portfolioId===selected.portfolioId?"SELECTED":item.status}</b></button>)}</div><aside className="hedge-optimizer__evidence"><span className="section-kicker">Selected evidence</span><h3>{portfolio.portfolioId}</h3><dl><div><dt>Instruments</dt><dd>{portfolio.candidateIds.join(" · ")}</dd></div><div><dt>Coverage</dt><dd>{percent(portfolio.coverageBps)}</dd></div><div><dt>Stress payoff</dt><dd>{money(portfolio.stressPayoffMinor)}</dd></div><div><dt>Basis correlation</dt><dd>{percent(portfolio.weightedBasisCorrelationBps)}</dd></div><div><dt>Counterparty max</dt><dd>{percent(portfolio.maximumCounterpartyConcentrationBps)}</dd></div><div><dt>Policy outcome</dt><dd>{portfolio.status}</dd></div></dl><div className="hedge-optimizer__boundary"><LockKeyhole size={15}/><span>{portfolio.reasonCodes.length?portfolio.reasonCodes.join(" · "):"All deterministic gates passed."}</span></div></aside></div><div className="hedge-optimizer__governance"><ShieldCheck size={18}/><div><span className="section-kicker">Published simulation plan</span><strong>{seededPublishedHedgePlan.planId} · {seededPublishedHedgePlan.makerId} → {seededPublishedHedgePlan.checkerId}</strong><small>No broker · no execution · no money movement</small></div><button type="button" onClick={draft}>Draft re-optimisation</button></div>{action!=="idle"&&<div className={`customer-review-result customer-review-result--${action==="drafted"?"submitted":"denied"}`} role="status"><strong>{action==="drafted"?"Re-optimisation drafted":"Permission denied"}</strong><span>{action==="drafted"?"A new paper plan requires a different checker; the published simulation remains unchanged.":"This principal cannot initiate hedging governance."}</span></div>}</section>}
