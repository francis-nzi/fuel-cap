const url=process.env.HEALTH_URL;const expected=(process.env.EXPECTED_COMMIT??"").slice(0,7);if(!url||!expected)throw new Error("HEALTH_URL and EXPECTED_COMMIT are required.");const response=await fetch(url,{headers:{accept:"application/json"}});if(!response.ok)throw new Error(`Health returned ${response.status}.`);const body=await response.json();if(body.status!=="ok"||body.commit!==expected||body.provenance!=="synthetic-seeded")throw new Error(`Health provenance mismatch: ${JSON.stringify(body)}`);console.log(JSON.stringify({status:"PASS",commit:body.commit,service:body.service}));
